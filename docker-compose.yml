version: '2.1'

services:
  errbot:
    image: errbot:alpine
    build: 
      context: .
    environment:
      - BACKEND=Slack
      - BOT_ADMINS=@winuim
      - SLACK_BOT_TOKEN
    stdin_open: true
    tty: true
